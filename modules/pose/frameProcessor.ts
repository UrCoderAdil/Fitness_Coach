/**
 * Vision Camera frame processors run MediaPipe on the native thread.
 * Install `react-native-vision-camera`, create a dev client (`npx expo prebuild`),
 * and plug your native `detectPose(frame)` worklet here.
 *
 * @example
 * ```ts
 * import { useFrameProcessor } from 'react-native-vision-camera';
 * import { runOnJS } from 'react-native-reanimated';
 * ```
 */
export const FRAME_PROCESSOR_README =
  'See README Phase 1 — wire detectPose from native MediaPipe Landmarker.';
